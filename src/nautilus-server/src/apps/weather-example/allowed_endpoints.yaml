# External endpoints that the enclave is allowed to access. 
endpoints:
  - api.weatherapi.com # replace with your own endpoints
  - api.weatherapi.com # replace with your own endpoints
allowed_endpoints:
  - domain: api.weatherapi.com
    port: 443
